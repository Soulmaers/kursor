<div class="container_flash">
   
        <p class="log role" data-att="<%=uniqIDCreater%>" rel="<%=role%>"><% if (role==='Курсор' ) { %>
        <i class="settings fas fa-user-cog"></i>
            <% } else { %>
        <i class="settings fas fa-user"></i>
                <% } %></p>
            <p class="log us"><%=user%></p>
            <div class="page-header text-center">
                <a href="/logout" class="logout"><i class="logoutIcon fas fa-sign-in-alt"  rel="Выход"></i></a>
            </div>
    
</div>